import random

# ======== ========= ========= ========= ========= ========= ========= =========

def rnd_list(lst):
    index = random.randint(0, len(lst)-1)
    return lst[index]

# ======== ========= ========= ========= ========= ========= ========= =========
